@startuml
left to right direction
skinparam node {
    BackgroundColor White
    BorderColor Black
}

node "Dispositivo do Usuário" as client {
    node "Navegador Web" as browser {
        artifact "Frontend (HTML/JS/CSS)" as frontend
    }
}

cloud "Internet/Rede Mackenzie" as network

node "Servidor de Aplicação (Linux/Cloud)" as server {
    node "Java Runtime Environment (JRE)" as jvm {
        artifact "sgo-app.jar <<Monolito Modular>>" as artifact {
            component "Módulo Competições" as mod_comp
            component "Módulo Inscrições" as mod_insc
            component "Módulo Alocação" as mod_aloc
            component "Módulo Resultados" as mod_rel
        }
    }
}

node "Servidor de Banco de Dados" as db_server {
    database "PostgreSQL" as db {
        artifact "Schema SGO" as schema
    }
}

' Comunicações
browser -- network : "HTTPS"
network -- server : "HTTPS"
artifact -- db_server : "JDBC (TCP/IP)"

note bottom of artifact
  Desenvolvido em Java.
  Módulos compartilham a mesma 
  memória e conexão com o banco.
end note

note top of server
  Arquitetura simplificada:
  Menor latência e facilidade
  de deploy para o trabalho.
end note

@enduml